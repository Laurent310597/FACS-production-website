# Changelog v20.1 — Full Image Display

- Sửa ảnh bìa trang chi tiết từ `object-cover` và giới hạn chiều cao sang `object-contain`, chiều cao tự động.
- Sửa ảnh xem trước tại Admin để hiển thị toàn bộ ảnh dọc và infographic.
- Giữ nguyên cách crop thumbnail trên trang danh sách Insights để các card đồng đều.
- Thay `package-lock.json` bằng bản dùng npm public registry, tránh lỗi cài dependency trên Vercel.
